---
sidebar_position: 1
title: OpenAPI Specification
description: Define an OpenAPI specification that describes the API contract before writing any implementation code, using the envelope pattern for consistent responses.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GithubCommitsInfo from '@site/src/components/github-commits-info';
import {
  FilmStructureJson,
  FileTree,
  EnvelopePatternJson,
  ResponseMetadataSchemaYaml,
} from '@site/src/components/docs/openapi-spec-first-development/openapi-spec.tsx';

<GithubCommitsInfo
  commits={[
    {
      name: 'OpenAPI Specifications',
      url: 'https://github.com/franBec/springboot-demo-projects/commit/2cce680239480e99bb910b5864ba3699f2ef4b20',
    },
  ]}
/>

Now we need an OpenAPI specification that describes the API contract before we write any implementation code. Given a Film with the following structure:

<FilmStructureJson />

We want our Spring Boot application to expose [CRUD endpoints](https://en.wikipedia.org/wiki/Create,_read,_update_and_delete)

## Changed files

<FileTree />

## OpenAPI Specification

> The [OpenAPI Specification (OAS)](https://swagger.io/specification/) defines a standard, language-agnostic interface to HTTP APIs which allows both humans and computers to discover and understand the capabilities of the service without access to source code, documentation, or through network traffic inspection. When properly defined, a consumer can understand and interact with the remote service with a minimal amount of implementation logic.

<Tabs groupId="language" queryString>
    <TabItem value="java" label="Java" default>

        ```yml reference title="resources/openapi.yaml"
        https://raw.githubusercontent.com/franBec/springboot-demo-projects/2cce680239480e99bb910b5864ba3699f2ef4b20/spring_java/src/main/resources/openapi.yaml
        ```

    </TabItem>
    <TabItem value="kotlin" label="Kotlin">

        ```yml reference title="resources/openapi.yaml"
        https://raw.githubusercontent.com/franBec/springboot-demo-projects/2cce680239480e99bb910b5864ba3699f2ef4b20/spring_kotlin/src/main/resources/openapi.yaml
        ```

    </TabItem>
    <TabItem value="groovy" label="Groovy">

        ```yml reference title="resources/openapi.yaml"
        https://raw.githubusercontent.com/franBec/springboot-demo-projects/2cce680239480e99bb910b5864ba3699f2ef4b20/spring_groovy/src/main/resources/openapi.yaml
        ```

    </TabItem>

</Tabs>

For better visualization you can copy-paste the OpenAPI Specification YAML file from the codebase into [Swagger Editor](https://editor.swagger.io/) or use [OpenAPI (Swagger) Editor
in IntelliJ IDEA](https://plugins.jetbrains.com/plugin/14837-openapi-swagger-editor).

## The envelope pattern

You might be looking at the response examples and thinking, "What's with all that extra fluff? Why wrap the actual film data inside a `data` object?"

<EnvelopePatternJson />

That is a deliberate choice called the **Envelope Pattern**. And once you start using it, you won't want to go back.

Think of it like physical mail. Every piece of mail you get, whether it's a birthday card or a bill, comes in an envelope. The envelope has standard information on it: a return address, a destination address, a stamp. The actual message is _inside_.

Our API responses work the same way. The `ResponseMetadata` schema is our envelope.

<ResponseMetadataSchemaYaml />

Every single response from the API, whether it's a success (`200 OK`) or an error (`404 Not Found`, `500 Internal Server Error`, etc.), will have this same top-level structure.

- The actual payload goes inside the `data` property for successful responses.
- For errors, we swap `data` for `title` and `detail` so we keep adhering to [Problem Details for HTTP APIs](https://datatracker.ietf.org/doc/html/rfc7807).

### Why even bother?

Consistency, that's the whole game.

1.  **Client side sanity:** The developer building the frontend or mobile app can write _one_ piece of code to handle all API responses. They always know where to find the `trace` ID to show a user reporting a bug. They can build a generic error handler that always looks for `title` and `detail`. They aren't guessing whether the payload is the object itself, an array, or some weird error shape.
2.  **Future proofing:** What if we need to add some other metadata? No problem. We just add a new field to the envelope level. The `data` part remains untouched, and we don't break the client's parsing logic for the actual film data.
3.  **It just looks professional:** An API with a consistent response structure feels solid and well-thought-out. An inconsistent one feels amateurish and is a pain to work with.

## Pagination

TODO: explain pagination

With the OpenAPI specification and the envelope pattern in place, the API contract is fully defined. The next step is to generate Spring Boot code from this spec so the implementation matches the contract exactly.
